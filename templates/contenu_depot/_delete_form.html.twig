<form method="post" action="{{ path('contenu_depot_delete', {'id': contenu_depot.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contenu_depot.id) }}">
    <button class="btn">Delete</button>
</form>
