{% extends 'base.html.twig' %}

{% block title %}Depots{% endblock %}

{% block body %}
    <h1>Depots</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ depot.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ depot.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('depots_index') }}">back to list</a>

    <a href="{{ path('depots_edit', {'id': depot.id}) }}">edit</a>

    {{ include('depots/_delete_form.html.twig') }}
{% endblock %}
